/** Legacy selection pill — replaced by LayerWindow. Kept for API compatibility. */
export function SelectionPanel() {
  // LayerWindow now handles all selection UI — this component is retired
  return null;
}
